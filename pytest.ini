[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
pythonpath = .
[pytest]
markers =
    integration: marks tests as integration tests (requiring external services like Redis)
    unit: marks tests as unit tests (no external dependencies)
